<% 
session_token = op signin --account basecamp --raw.strip
if session_token.empty? 
  raise ArgumentError, "Session token missing"
end
%>
# Generated by mrsk envify
MRSK_REGISTRY_PASSWORD=<%= op read "op://Deploy/Docker Hub MRSK/password" -n --session #{session_token} %>
RAILS_MASTER_KEY=<%= op read "op://Development/Chat-in-a-box/RAILS_MASTER_KEY" -n --session #{session_token} %>
HOST_URL=https://unpartaking-unvengeful-tama.ngrok-free.dev
WEBHOOK_URL=https://nowcompany.app.n8n.cloud/webhook-test/financial-chat